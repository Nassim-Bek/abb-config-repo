spring:
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: cc-aggregation
              uri: lb://cc-aggregation-service
              predicates:
                - Path=/cc/**
            - id: cc-aggregation-openapi
              uri: lb://cc-aggregation-service
              predicates:
                - Path=/v3/api-docs/**,/v3/api-docs.yaml,/swagger-ui/**,/swagger-ui.html
          httpclient:
            connect-timeout: 3000
            response-timeout: 30s
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus,gateway
